declare const SET_COOKIE: unique symbol;
declare global {
    interface Headers {
        [SET_COOKIE]?: string[];
        raw?(): Record<string, string | string[]>;
        getAll?(name: string): string[];
    }
}
declare function install(): void;

export { install as default };

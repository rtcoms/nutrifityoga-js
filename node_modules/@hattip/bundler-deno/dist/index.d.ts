import { BuildOptions } from 'esbuild';

/**
 * Bundling options
 */
interface BundlingOptions {
    /**
     * Deno module entry point.
     */
    input: string;
    /** Output file name for the bundle */
    output: string;
    /**
     * Static files directory to copy next to the output.
     * @default undefined
     */
    staticDir?: string;
}
declare function bundle(options: BundlingOptions, manipulateEsbuildOptions?: (options: BuildOptions) => void | Promise<void>): Promise<void>;

export { BundlingOptions, bundle as default };
